name: Deploy camera server

on: workflow_dispatch

jobs:
  deploy:
    uses: ./.github/workflows/run_ansible.yaml
    with:
      command: uv run ansible-playbook -i ansible/inventory.yaml ansible/playbooks/camera_server.yaml
    secrets: inherit