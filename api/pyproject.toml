[project]
name = "api"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "memoization>=0.4.0",
    "pydantic-settings>=2.10.1",
]
